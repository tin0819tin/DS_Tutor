// Initialization
let DS = []
const OM = new ObjectManager();
const AM = new AnimationManager(OM, document, DS[0]);
AM.DIYMode = false;

// Random the DS question and check the answer

const startProblem = () => {
    
}

